{
  "name": "@novilearn/types",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": ["src", "dist"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf node_modules dist"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "@novilearn/config": "workspace:*"
  }
}